using Catharsium.Math.Geometry.Calculators;
using Catharsium.Math.Geometry.Interfaces;
using Catharsium.Math.Geometry.Models;
using Catharsium.Util.Testing;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using NSubstitute;

namespace Catharsium.Math.Geometry.Tests.Calculators.AreaCalculatorTests
{
    [TestClass]
    public class GetAreaTriangleTests : TestFixture<AreaCalculator>
    {
        [TestMethod]
        public void GetArea_()
        {
            //var triangle = new Triangle();
            //this.GetDependency<ICircumferenceCalculator>().GetCircumference(triangle).Returns(1);
            //var actual = this.Target.GetArea(triangle);
            // O^2 = s * (s - a) * (s - b) * (s - c)
        }
    }
}